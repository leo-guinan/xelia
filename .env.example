# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/xelia

# Session Configuration (REQUIRED - Generate a strong random string)
# Generate with: openssl rand -base64 32
SESSION_SECRET=

# Plaid API Configuration
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=development # sandbox, development, or production

# Server Configuration
PORT=5000
NODE_ENV=development # development or production

# Security Configuration (Recommended for production)
ENCRYPTION_KEY= # For encrypting sensitive data
ALLOWED_ORIGINS=http://localhost:5000 # Comma-separated list for CORS
RATE_LIMIT_WINDOW_MS=900000 # 15 minutes
RATE_LIMIT_MAX_REQUESTS=5 # Max requests per window for auth endpoints

# Email Configuration (For future email verification)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=noreply@xelia.com